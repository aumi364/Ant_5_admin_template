import React from 'react';
import MenuItemCard from '../../components/card/menuItemCard';

const Items = props => {
  return (
    <div>
      <MenuItemCard />
    </div>
  );
};

Items.propTypes = {};

export default Items;
